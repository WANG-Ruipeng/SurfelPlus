// src/surfel_prepare.comp
#version 450

layout(set = 0, binding = 0) buffer SurfelCounterBuffer {
    uint gSurfelCounter[];
};

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {
    const uint kTotalSurfelLimit = 150000; 
}
